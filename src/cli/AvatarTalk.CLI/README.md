# AvatarTalk.CLI

Command-line interface for the AvatarTalk SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global AvatarTalk.CLI --prerelease
```

## Usage

```bash
avatar-talk --help
avatar-talk api --help
```